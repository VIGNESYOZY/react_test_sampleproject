version: '0.2'
phases:
  install:
    runtime-versions:
      nodejs: 22.14.0
    commands:
      - echo Installing dependencies...
      - npm install --force
  pre_build:
    commands:
      - echo Building project...
      - npm run build --verbose
  build:
    commands:
      - echo ID_UQw9j12F
      - echo Creating tar.gz archive of build files...
      - cd build
      - tar -czvf ../PLN2000005.tar.gz *
  post_build:
    commands:
      - echo Pushing artifacts...
artifacts:
  files:
    - PLN2000005.tar.gz
